version: "{build}-{branch}"

platform:
  - Win32

init:
  - cmake --version

before_build:
  - md build
  - cd build
  - cmake .. -DCMAKE_BUILD_TYPE="Release" -DCMAKE_CXX_FLAGS="/O2 /W3" -DMAKE_TESTS="yes"

after_build:
  - ctest
