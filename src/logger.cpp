#include "logger.h"

std::ostream* erebos::logger::Logger::current_stream = nullptr;

void erebos::logger::Logger::error(const std::string& message,
	const log_type& ty_log) {

}

void erebos::logger::Logger::warn(const std::string& message,
	const log_type& ty_log) {

}

void erebos::logger::Logger::info(const std::string& message,
	const log_type& ty_log) {

}